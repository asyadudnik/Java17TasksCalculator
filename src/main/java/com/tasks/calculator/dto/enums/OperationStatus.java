package com.tasks.calculator.dto.enums;

public enum OperationStatus {

    START, //during creation task
    RUN, //adding/removing of operation
    DONE; //in case all operation are in the status Done

    }
